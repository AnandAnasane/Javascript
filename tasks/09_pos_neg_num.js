// Check number is positive or Negative

//  1. with if and else

const num = 45 ;

if(num%2 == 0) console.log(`${num}  is a positive number`);
else console.log(`${num} is a Negative number`);


// with ternary operator

const a = 110;
const result = (a%2==0) ? `${a} is a positive number` : `${a} is a Negative number`;
// condition ? true : false ;
console.log(result);


